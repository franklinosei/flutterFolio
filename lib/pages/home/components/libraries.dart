import 'package:flutter/material.dart';

class Libraries extends StatelessWidget {
  const Libraries({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('images of libraries'),
    );
  }
}

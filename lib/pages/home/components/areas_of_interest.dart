import 'package:flutter/material.dart';

class AreasOfInterest extends StatelessWidget {
  const AreasOfInterest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Areas of interest'),
    );
  }
}
